import React from 'react';
import SearchBar from '../GPTSearch/SearchBar';

const GPTSearch = () => {
    return (
        <div className='text-white w-screen relative h-screen'>
            <SearchBar />
        </div>
    );
}

export default GPTSearch;
